# macOS related thing (i dont like it)
.DS_Store

# Node.js dependencies
node_modules

# Node.js package lock files
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lock
bun.lockb

# Build output
dist

# UFS2 image (256MB, generated via Docker/CI — too large for GitHub)
download0.dat